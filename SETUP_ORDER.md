# ترتيب تطبيق ملفات SQL في Supabase

شغّل الملفات بالترتيب التالي في Supabase SQL Editor:

## المرحلة الأولى: الإعداد الأساسي
1. `supabase_setup_fixed.sql` — الجداول الأساسية والإعدادات

## المرحلة الثانية: الجداول الإضافية
2. `new_tables_setup.sql` — جداول platform_events, rewards_tiers
3. `add_xp_reward_points.sql` — أعمدة xp و reward_points
4. `anti_spam_points.sql` — نظام مكافحة الاحتيال interaction_logs
5. `update_points_daily_limit.sql` — تحديث الحد اليومي للنقاط إلى 100
6. `fin_setup.sql` — جداول الإدارة المالية

## المرحلة الثالثة: الأمان والصلاحيات (مهم: الترتيب مهم)
7. `fix_rls_secure.sql` — تشديد سياسات RLS
8. `fix_dashboard_permissions.sql` — **يجب بعد رقم 7 مباشرة** لإضافة SELECT للـ anon على analytics
9. `fix_profiles_admin.sql` — ربط حساب الإدارة (غيّر UUID فيه لـ UUID حسابك)

## المرحلة الرابعة: نظام الاشتراكات
10. `join_subscription_setup.sql` — جداول نظام الاشتراك (باقات، أكواد خصم، طلبات اشتراك)
11. `refund_requests_setup.sql` — جدول طلبات الاسترجاع

## المرحلة الخامسة: الرسائل الهامة والاستطلاعات ✅ (مُطبَّقة)
12. `announcements_surveys_setup.sql` — جداول:
    - `announcements` + `announcement_views` (الرسائل الهامة: بانر عادي / نافذة عاجلة)
    - `surveys` + `survey_responses` + `survey_dismissals` (استطلاعات رأي بعد فترة من الاشتراك)

## ملاحظات مهمة
- ⚠️ رقم 8 يجب دائماً بعد رقم 7 وإلا لوحة المدرس لن ترى الإحصائيات
- ⚠️ رقم 9 يحتاج تعديل UUID حساب الأدمن قبل التشغيل
- ✅ يمكن إعادة تشغيل أي ملف بأمان (كلها تستخدم IF NOT EXISTS / ON CONFLICT DO NOTHING)
- ✅ رقم 12 تم تطبيقه بالفعل مباشرة على قاعدة البيانات عبر Supabase MCP، الملف محفوظ للمرجعية فقط

## نظرة عامة على نظام الرسائل الهامة (Announcements)
- تُدار بالكامل من لوحة الإدارة → "الرسائل الهامة"
- نوعان: **عادية** (بانر أعلى لوحة المدرس) أو **عاجلة** (نافذة منبثقة لازم المدرس يقفلها)
- تحكم كامل في: تاريخ البداية، تاريخ النهاية (أو بدون)، عدد مرات الظهور المسموحة لكل مدرس، رابط إضافي اختياري
- الرسائل العاجلة بمجرد ما المدرس يضغط "فهمت" بيتسجل `dismissed=true` وما تظهرش تاني لنفس المدرس

## نظرة عامة على نظام الاستطلاعات (Surveys)
- تُدار من لوحة الإدارة → "الاستطلاعات والتقييم"
- كل استطلاع له: عنوان، وصف، تفعيل تقييم النجوم (اختياري)، عدد الأيام بعد الاشتراك اللي يظهر بعدها
- الأسئلة قابلة للتخصيص بالكامل: نص السؤال + النوع (اختيار من متعدد بخيارات محددة، أو نص حر)
- يظهر للمدرس تلقائياً في لوحته بعد مرور `trigger_days_after_subscription` يوم من `teacher.subscription_start`
- لو المدرس ضغط "لاحقاً" ميتكررش ظهور نفس الاستطلاع له مرة أخرى
- نتائج الاستطلاعات (تقييم النجوم + إجابات الأسئلة) تظهر في لوحة الإدارة مجمّعة لكل استطلاع
