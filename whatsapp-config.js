/* ============================================================
   مدرسك — إعدادات ويدجت واتساب (ملف الإعدادات الوحيد)
   عدّل القيم هنا فقط؛ لا داعي لتعديل whatsapp.js أو whatsapp.css
   ============================================================ */
window.MDRSK_WA_CONFIG = {

  // رقم الواتساب — يُقرأ تلقائيًا من CONTACT_WHATSAPP في config.js
  // الأساسي لو كان محمَّلاً قبل هذا الملف في نفس الصفحة، وإلا يُستخدم
  // الرقم الاحتياطي هنا (بأي صيغة: بمسافات أو +، الكود بيصفّيها بنفسه)
  phone: (typeof CONFIG !== 'undefined' && CONFIG.CONTACT_WHATSAPP) || '+20 10 80474084',

  // اسم الحساب الذي يظهر أعلى نافذة الدردشة
  accountName: 'فريق مدرسك',

  // نص الحالة تحت الاسم
  statusText: 'متصل الآن',

  // مسار شعار الحساب (نفس logo.png المستخدم في باقي الموقع)
  logo: 'logo.png',

  // الرسالة الترحيبية داخل فقاعة الدردشة (استخدم \n لسطر جديد)
  welcomeMessage: '👋 أهلاً بك في مدرسك.\nكيف يمكننا مساعدتك اليوم؟',

  // الرسالة الجاهزة التي تُرسل تلقائيًا عند فتح واتساب
  prefilledMessage: 'السلام عليكم، أريد الاستفسار عن منصة مدرسك.',

  // نص زر بدء المحادثة
  ctaLabel: 'ابدأ المحادثة عبر واتساب',

  // إظهار نقطة تنبيه صغيرة على الزر العائم قبل أول تفاعل من الزائر
  showBadge: true,

  // بعد كام ثانية من دخول الزائر تفتح النافذة تلقائيًا (لو ما قفلهاش قبل كده)
  autoOpenDelaySec: 8,

  // كل كام ثانية تتكرر حلقة التنبيه حول الزر — تتوقف تلقائيًا أول ما الزائر يتفاعل
  pulseIntervalSec: 15,

  // مفتاح التخزين المحلي لحالة الإغلاق — غيّره لو حبيت تصفّر حالة كل الزوار القدامى
  storageKey: 'mdrsk_wa_widget_v1',
};
