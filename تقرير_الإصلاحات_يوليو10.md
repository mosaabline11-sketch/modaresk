# تقرير الإصلاحات — مدرسك
**التاريخ:** 10 يوليو 2026

هذا التقرير يشرح كل مشكلة من التسعة اللي بعتهم، وايه السبب الحقيقي اللي لقيته، وايه الحل اللي طبقته. فيه حاجات كانت أعمق من مجرد "باج بسيط" فى الواجهة — جزء كبير من المشاكل كان سببه صلاحيات قاعدة البيانات بعد التدقيق الأمني الأخير (الـ13 migration).

---

## 1) تداخل صندوق البحث مع اختيار المادة (الصفحة الرئيسية)
**السبب:** غلطتين في CSS مع بعض:
- أيقونة 🔍 وزر المايك 🎤 كانوا متمركزين (`position:absolute`) على نفس البقعة تقريبًا جوه صندوق البحث، فبيتراكبوا فوق بعض.
- تعارض في أولوية CSS خلّى قائمتي "المادة" و"الصف" ياخدوا حجم أكبر من المفروض (كأنهم `flex:2` بدل `flex:1`)، فبيزاحموا صندوق البحث جنبهم.

**الحل:** نقلت زر المايك لأقصى الطرف التاني (بعيد عن أيقونة 🔍)، وصلّحت تعارض الأولوية عشان القوائم ترجع للحجم الصحيح، وحطّيت حد أدنى لعرض صندوق البحث نفسه.
📄 `index.html`

---

## 2) إعلان "المرحلة كاملة" ما يظهرش لما تدور على صف معين فيها
**السبب:** مش باج في الكود فعليًا — لقيت إعلان فعلي في قاعدة البيانات كان محفوظ فيه `grade = "المرحلة الابتدائية كاملة"` (النص نفسه) بدل الأسماء الستة الفعلية للصفوف (أول ابتدائي، ثاني ابتدائي...). البحث بيقارن بالأسماء الفعلية للصفوف، فمش هيلاقي تطابق مع نص عام زي ده.

**الحل:**
- صلّحت بيانات الإعلان ده في قاعدة البيانات (رجّعتله الصفوف الستة الفعلية).
- ضفت قيد حماية (`CHECK constraint`) على جدول `ads` يمنع تسجيل أي قيمة غير الصفوف المعتمدة الاثني عشر مستقبلًا، حتى لو حصل إدخال يدوي أو خطأ برمجي تاني.
- لقيت كمان إعلان تجريبي تاني بقيمة "الصف الثامن" (مش من ضمن نظام مدرسك أصلًا) وصلّحته لـ"ثاني إعدادي" بناءً على عنوان الإعلان نفسه.

🗄️ قاعدة البيانات (migration + تصحيح بيانات)

---

## 3) "تعديل إعلان" من الصفحة الرئيسية للوحة كان بيفتح آخر إعلان مباشرة
**الحل:** لو عند المدرس أكتر من إعلان واحد، الزر دلوقتي بيفتح نافذة صغيرة تعرض كل إعلاناته (بالعنوان والحالة) عشان يختار هو أنهي واحد يعدّله. لو عنده إعلان واحد بس، بيفتحه على طول زي ما كان (مفيش خطوة زيادة من غير داعي).
📄 `dashboard_logic.js` + `dashboard.html` (نافذة جديدة "اختر إعلانًا")

---

## 4) رسالة خطأ في تبويب "التحليلات" بلوحة المدرس
**السبب:** التدقيق الأمني الأخير سحب صلاحية قراءة جدول `analytics_events` بالكامل من مفتاح anon (اللي بيستخدمه المدرسين) عشان يمنع أي حد يقرا كل بيانات كل المدرسين — لكن ده قطع في نفس الوقت قدرة لوحة المدرس على قراءة **إحصائياته هو نفسه**. النتيجة: أي محاولة تحميل التحليلات كانت بترجع "Permission denied".

**الحل:** رجّعت صلاحية قراءة محدودة جدًا — بس الأعمدة اللي فعلًا محتاجاها اللوحة (نوع الحدث، التاريخ، رقم الإعلان، رقم المدرس)، من غير ما نرجع نكشف أعمدة زي user agent أو session id لأي حد.
🗄️ قاعدة البيانات (migration)

---

## 5) الإشعارات بتفضل تظهر "غير مقروءة" حتى بعد ما تعلّمها كمقروءة
**السبب:** زر "تعليم كمقروء" كان بس بيخفي الرقم الأحمر بصريًا للحظتها، من غير ما يحفظ أي حاجة فعلًا. أي إعادة تحميل للوحة كانت بتعتبر كل الإشعارات غير مقروءة تلقائيًا لأنه معدش تسجيل مين قرا ايه أصلًا.

**الحل:** أنشأت جدول جديد `notification_reads` يسجّل فعليًا مين قرا انهي إشعار ومتى، وربطت زر "تعليم كمقروء" وعداد الجرس بيه. دلوقتي الحالة بتفضل صح حتى لو قفلت المتصفح ورجعت.
🗄️ قاعدة البيانات (جدول جديد) + 📄 `dashboard_logic.js`

---

## 6) زر "عرض ملفي للطلاب" كان بيفتح آخر إعلان دايمًا
**الحل:** نفس منطق نقطة 3 — لو عنده أكتر من إعلان، بيديله نافذة اختيار بدل ما يفتح إعلان عشوائي (آخر واحد) بدون سؤاله.
📄 `dashboard_logic.js`

---

## 7) تسجيل الدخول كمدرس ما بيفضلش شغال بعد إغلاق المتصفح / في تابات تانية
**السبب:** جلسة المدرس كانت متخزنة في `sessionStorage`، وده معناه:
- بتتمسح بمجرد ما تقفل التاب أو المتصفح.
- كل تاب جديد ليه نسخته الخاصة (فاضية)، حتى لو انت مسجل دخول في تاب تاني من نفس الموقع.

كمان لقيت مشكلة أعمق: بعد التدقيق الأمني، صفحة الدخول (`login.html`) كانت لسه بتحاول تتحقق من اسم المستخدم وكلمة المرور بقراءة مباشرة من جدول `teachers` — لكن التدقيق سحب صلاحية قراءة عمودي username/password_hash تمامًا (وده صح أمنيًا، عشان محدش يقدر يسحب كلمات المرور المشفّرة)، وأنشأ بدالها دالة آمنة (RPC) اسمها `teacher_login`. المشكلة إن صفحة الدخول معملتش عليها تحديث لاستخدام الدالة الجديدة، يعني تسجيل الدخول من الأساس كان معرّض يفشل.

**الحل:**
- حدّثت `login.html` تستخدم دالة `teacher_login` الآمنة بدل القراءة المباشرة.
- غيّرت تخزين الجلسة من `sessionStorage` لـ `localStorage` مع مدة صلاحية 30 يوم (بتتجدد تلقائيًا مع أي نشاط) — كده هتفضل مسجل دخول عبر كل التابات وحتى لو قفلت المتصفح وفتحته تاني.
📄 `login.html` + `config.js`

---

## 8) مشاكل تصميم في الرسائل الهامة
**السبب اللي قدرت أتأكد منه:** لقيت رسالة تجريبية عنوانها رقم طويل متواصل من غير مسافات ("2222222222222222222222222")، وده نوع النصوص اللي بتكسر تصميم الهيدر بتاع النافذة العاجلة لأنه معملوش `overflow-wrap`، فبيطلع بره حدود الصندوق أو يتقطع بشكل مبعثر.

**الحل:** ضفت حماية `overflow-wrap` لعنوان النافذة العاجلة وعنوان الاستطلاع، بحيث أي نص طويل (رقم، كلمة إنجليزي طويلة، إلخ) يلف جوه حدود النافذة بدل ما يكسرها.
📄 `style.css`

> ⚠️ لو المشكلة اللي شايفها حضرتك حاجة تانية غير كده (لأني مش شايف الصورة اللي كنت تقصدها)، ابعتلي سكرين شوت وأظبطها بالظبط.

---

## 9) الرسائل الهامة بتظهر مرة واحدة بس حتى لو محدد إنها تظهر 3 مرات
**السبب:** لقيته بالظبط من بيانات قاعدة البيانات — أي رسالة من نوع "عاجلة" (النافذة المنبثقة)، أول ما المدرس يضغط "فهمت"، الكود كان بيسجّلها "مُغلقة نهائيًا" (`dismissed = true`) فورًا، بغض النظر عن قيمة "عدد مرات الظهور" اللي انت حددتها في لوحة الإدارة. يعني حتى لو حطيت 3، هي عمليًا بتتقفل نهائيًا من أول ظهور.

**الحل:** شلت السطر اللي كان بيسجّل "إغلاق نهائي" عند الضغط على "فهمت". دلوقتي "عدد مرات الظهور" اللي بتحدده هو اللي بيتحكم فعليًا في متى تختفي الرسالة، للرسائل العادية والعاجلة على حد سواء. وصفّرت حالة الرسالة التجريبية اللي كانت عندك عشان تقدر تجربها فورًا بعد الرفع.
📄 `config.js` + تصحيح بيانات تجريبية

---

## ملفات محتاجة رفع يدوي على GitHub Pages
```
index.html
login.html
join.html
dashboard.html
dashboard_logic.js
config.js
style.css
```
كل الملفات دي رقم الإصدار فيها اتحدّث لـ `v=7.9.0` عشان يجبر المتصفح ياخد النسخة الجديدة. بعد الرفع، اعمل Ctrl+Shift+R على أي صفحة بتفتحها للتجربة.

**كل التعديلات على قاعدة البيانات اتطبقت فعليًا دلوقتي على Supabase — مفيش حاجة تانية مطلوبة منك في الداتابيز.**

---

## حاجات لقيتها زيادة عن الـ9، تستاهل انتباهك (مش أخطاء بتمنع الاستخدام، بس مخاطر تستاهل تعرفها)

1. **أي حد يقدر يعدّل نقاط XP/المكافآت بتاعة أي مدرس عن طريق استدعاء مباشر لـ API** (مش من الواجهة العادية) — لأن نظام حماية النقاط كله من جهة المتصفح فقط. ده قايم من الأول مش حاجة جديدة، بس حبيت تكون عارفها. الحل الكامل ليها محتاج نقل تسجيل دخول المدرسين لنظام Supabase Auth الحقيقي بدل الطريقة الحالية، وده مشروع أكبر من مجرد إصلاح.
2. **مجلد الصور المرفوعة (`uploads` bucket) بيسمح لأي حد يشوف قائمة كل الملفات فيه** (مش بس يفتح صورة بمعرفتها). مش خطير جدًا لأن الصور أصلًا عامة، بس ينفع نراجعه من إعدادات Supabase Storage.
3. **حماية كلمة مرور الإدارة من قوائم كلمات السر المسربة** — خاصية جاهزة في Supabase، تقدر تفعّلها من إعدادات Auth بدون أي تعديل كود.
4. **لقيت إعلانات تجريبية كتير في قاعدة البيانات فيها عناوين زي "test"، "تجربة"، حروف عشوائية.** ماشيتهاش عشان مش متأكد لو لسه محتاجها للتجربة، بس لو خلصت منها قولّي أنضّفها.
