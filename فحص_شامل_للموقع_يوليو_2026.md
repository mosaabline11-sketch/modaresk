# فحص شامل لمنصة مدرسك — يوليو 2026

**تاريخ الفحص:** 8 يوليو 2026
**نطاق الفحص:** كل صفحات الموقع (37 ملف HTML/JS/CSS)، قاعدة بيانات Supabase الحية بالكامل (السياسات، الصلاحيات، الدوال، الفهارس)، وكل ملفات SQL المرجعية.
**طريقة الفحص:** قراءة كل سطر كود يدويًا + فحص تركيبي فعلي (`node --check`) لكل كتلة JavaScript في الموقع + استعلام مباشر على قاعدة البيانات الحية (وليس فقط ملفات SQL الأرشيفية) لمعرفة الوضع الفعلي + تشغيل أدوات Supabase Advisor الرسمية للتحقق المستقل.

---

## 0) خلاصة تنفيذية — اقرأها أولاً

كان في المنصة **ثغرة أمنية خطيرة جدًا وحقيقية وقابلة للاستغلال فعليًا الآن**، ليست نظرية: أي شخص في العالم يعرف رابط موقعك (وبالتالي يملك مفتاح anon العام المكتوب صراحة في `config.js`) كان يستطيع، بدون تسجيل دخول وبدون أي حيلة، أن:

- **يقرأ كلمة مرور كل مدرس** (الهاش + عمود `plain_password` لو تم استخدامه مستقبلًا) وكل بياناته.
- **يعدّل بيانات أي مدرس** — يفعّل اشتراكه مجانًا للأبد، يغيّر رقم واتسابه ليحوّل تواصل الطلاب لنفسه، يوقف مدرسًا آخر.
- **ينشر إعلانًا مزيفًا يظهر مباشرة على الصفحة الرئيسية** بدون مراجعة الإدارة (بتخطي حالة "قيد المراجعة" تمامًا).
- **يحذف أو يعدّل أي إعلان** لأي مدرس.
- **يقرأ ويعدّل كل بيانات لوحة الإدارة المالية** (الإيرادات، المصروفات، أرقام هواتف العملاء المحتملين، التقييم الشهري) — رغم أن هذه اللوحة مخصصة للإدارة فقط.
- **يزوّر أكواد الخصم** (يحوّل خصم 20% إلى 100%).
- يقرأ إجابات استطلاعات الرأي الخاصة بكل المدرسين.

**تم إصلاح كل هذا فعليًا على قاعدة البيانات الحية أثناء هذا الفحص** (13 migration مطبّقة مباشرة عبر Supabase — التفاصيل الكاملة في القسم 1). تأكدت من كل إصلاح بثلاث طرق: قراءة السياسات الفعلية بعد التعديل، إعادة تشغيل فاحص الأمان الرسمي من Supabase، ومطابقة كل استعلام في الكود الفعلي (37 ملف) عمود‑بعمود مع الصلاحيات الجديدة حتى لا ينكسر أي شيء يعمل حاليًا.

### ⚠️ إجراء مطلوب منك الآن (خطوتان فقط)
الإصلاح الوحيد الذي يحتاج تدخل منك (لأني لا أملك صلاحية الرفع المباشر لـ GitHub):
1. ارفع نسخة `login.html` المُصححة المرفقة (تسجيل الدخول أصبح يمر عبر دالة آمنة `teacher_login` بدل قراءة كلمة المرور مباشرة).
2. ارفع نسخة `join.html` المُصححة المرفقة (استخدام كود الخصم أصبح يمر عبر دالة آمنة `redeem_discount_code`).

**بدون رفع هاتين الصفحتين:** تسجيل دخول المدرسين سيتعطل تمامًا (لأن قاعدة البيانات لم تعد تسمح بقراءة كلمة المرور بالطريقة القديمة)، وكود الخصم في صفحة `join.html` سيستمر بالعمل للعميل لكن عداد "عدد مرات الاستخدام" لن يزيد (يعني الأكواد تبقى قابلة للاستخدام بلا حدود). **يُرجى رفع الملفين في أقرب وقت.**

---

## 1) الأمان وقاعدة بيانات Supabase (القسم الأهم)

### 1.1 كيف حدثت المشكلة أصلًا (السبب الجذري)

بمراجعة السياسات الفعلية الحية (`pg_policies`) وليس فقط ملفات الـ SQL الأرشيفية، اكتشفت القصة الكاملة:

- `fix_rls_secure.sql` طبّق فعلاً تشديدًا جيدًا على 6 جداول أساسية فقط (`teachers`, `ads`, `analytics_events`, `site_settings`, `subjects`, `notifications`).
- لكن **كل جدول أُضيف بعد ذلك عبر ملف إعداد منفصل** (`fin_setup.sql`, `join_subscription_setup.sql`, `announcements_surveys_setup.sql`, `new_tables_setup.sql`, `anti_spam_points.sql`) **احتفظ بسياسته الأصلية المفتوحة بالكامل** ("`FOR ALL TO anon, authenticated USING (true)`") ولم يمر أبدًا بنفس مرحلة التشديد.
- والأخطر: على الجداول الست التي شُدّدت أصلًا، **جلسة لاحقة (يبدو أنها جلسة Claude سابقة، بدليل التسمية الاحترافية للسياسات الجديدة مثل `admin_all_ads` و`public_read_active_ads` التي تتحقق من `profiles.role='admin'` وتتحقق من صلاحية اشتراك المدرس) كتبت سياسات صحيحة تمامًا... لكنها لم تحذف السياسات القديمة المفتوحة!** بما أن PostgreSQL يجمع كل السياسات بمنطق "OR" (أي وجود سياسة واحدة مسموحة يكفي)، فإن وجود سياسة قديمة بـ `USING(true)` بجانب سياسة جديدة دقيقة **يُلغي عمليًا كل فائدة السياسة الجديدة**. كانت هذه بالضبط نفس الملاحظة المسجّلة في ذاكرتي من جلسات سابقة ("نظّف السياسات القديمة دائمًا بعد كل تشديد") — لكنها لم تُطبَّق فعليًا هذه المرة الأخيرة.

النتيجة: كان عندك **من نسختين إلى أربع نسخ متراكبة من السياسة على نفس الجدول** — نسخة قديمة مفتوحة بالكامل، ونسخة أحدث مصممة بعناية — والقديمة كانت تفوز دائمًا.

### 1.2 ماذا وجدت بالضبط، جدول بجدول (قبل الإصلاح)

| الجدول | المشكلة الفعلية المؤكدة (سياسة + صلاحية معًا) | الخطورة |
|---|---|---|
| `teachers` | 4 سياسات SELECT مكررة كلها `true` + سياستا UPDATE كلاهما `true,true` → أي زائر يقرأ **ويكتب** كل الأعمدة بما فيها `password_hash`, `plain_password`, `username`, `is_active`, `subscription_end`, `allow_advanced_stats` | 🔴 الأخطر في كل الموقع |
| `ads` | سياسات SELECT/INSERT/UPDATE/DELETE مكررة كلها بلا شرط → قراءة الإعلانات المرفوضة/المعلّقة، **نشر إعلان بحالة `active` مباشرة متخطيًا المراجعة**، تعديل أي إعلان (حتى تغيير `status` أو `teacher_id`)، حذف أي إعلان | 🔴 حرج جدًا |
| `fin_subscriptions`, `fin_expenses`, `fin_leads`, `fin_monthly_reviews` | سياسة واحدة `FOR ALL TO anon, authenticated USING(true)` على كل جدول (+ سياسة إضافية مكررة على fin_subscriptions) | 🔴 حرج (بيانات مالية + أرقام هواتف عملاء محتملين بالكامل) |
| `discount_codes` | UPDATE مفتوح بالكامل → أي شخص يحوّل أي كود خصم لنسبة 100% أو يلغي حد الاستخدام | 🟠 عالٍ |
| `platform_events`, `rewards_tiers` | سياسة `ALL true` واحدة لكل جدول (لا يوجد حتى سياسة مخصصة للإدارة!) → أي زائر يحذف/يزوّر الفعاليات أو مستويات المكافآت | 🟠 عالٍ |
| `analytics_events` | سياستا SELECT/INSERT إضافيتان بجانب السياسات الصحيحة الجديدة → قراءة كل بيانات التتبّع لكل مدرس/زائر، وإدخال أنواع أحداث عشوائية | 🟡 متوسط |
| `survey_responses` | SELECT مفتوح بالكامل → قراءة تقييمات وملاحظات كل المدرسين (يُفترض أنها للإدارة فقط) | 🟡 متوسط |
| `announcement_views` | UPDATE مفتوح بالكامل → إمكانية "إخفاء" إشعار عاجل لمدرس آخر أو التلاعب بعدّاد المشاهدات | 🟢 منخفض |
| `analytics_summary` (VIEW) | View بخاصية `SECURITY DEFINER` (تتجاوز RLS تمامًا) غير مستخدَم في أي صفحة إطلاقًا | 🟡 (تم حذفه) |
| `app_admins`, `app_sessions` | جدولان بهما RLS مفعّل لكن **بلا أي سياسة إطلاقًا** (٠ صف، غير مُستخدَمين في أي كود حاليًا) | ℹ️ معلومة فقط |

### 1.3 اكتشاف مهم: نظام مكافحة الاحتيال الحقيقي موجود... لكنه غير مُفعَّل!

وجدت في القاعدة دالة SQL كاملة `upsert_interaction_safe` (من `anti_spam_points.sql`) مصمّمة بعناية شديدة: تتحقق من التبريد (cooldown) والحد اليومي والسقف الكلي للنقاط **من طرف السيرفر** بشكل ذرّي (atomic) — بالضبط الحل الصحيح لمنع تزوير النقاط. **لكن لا يوجد أي استدعاء لهذه الدالة في أي ملف من ملفات الموقع الفعلية!** الكود الحالي (`trackEvent` في `config.js`) يعتمد بالكامل على فحص من طرف المتصفح (`AntiSpam` object في `localStorage`) — وهذا يُتخطى بسهولة تامة (فتح المتصفح بوضع خفي، أو مسح localStorage، أو استدعاء Supabase API مباشرة). أي أن **نظام مكافحة الاحتيال في النقاط لا يوفّر أي حماية حقيقية الآن**، وحتى لو كان يعمل، فإن ثغرة `teachers.points/xp/reward_points` (المذكورة أعلاه) كانت تتيح تجاوزه بالكامل بتعديل النقاط مباشرة. هذا موضّح بالتفصيل في التوصيات (قسم 5) لأنه يحتاج إعادة ربط، وليس مجرد تعديل صلاحيات.

### 1.4 الإصلاحات المُطبَّقة فعليًا الآن على القاعدة الحية (13 migration)

طبّقت هذه مباشرة عبر Supabase MCP وتحققت من كل واحدة على حدة، ثم أعدت تشغيل فاحص الأمان الرسمي من Supabase للتأكيد:

1. **`harden_teachers_credentials_and_login_rpc`** — حذف كل السياسات المكررة المفتوحة، إبقاء سياسة قراءة واحدة (`is_active=true` فقط، بدون شرط انتهاء الاشتراك حتى تبقى لوحة "جدّد اشتراكك" تعمل)، **تقييد الأعمدة على مستوى GRANT** بحيث لا يستطيع anon قراءة أو كتابة `username`/`password_hash`/`plain_password` إطلاقًا مهما كانت السياسة، وإنشاء دالة آمنة جديدة `teacher_login(username, password_hash)` (تعمل بصلاحيات مالك الجدول فتتجاوز RLS داخليًا فقط، وتُرجع الحقول الآمنة فقط).
2. **`harden_ads_status_and_ownership_columns`** — حذف كل السياسات المكررة، فرض `status='pending'` إجباريًا عند الإضافة عبر anon (بحيث لا يمكن نشر إعلان "نشط" مباشرة)، وتقييد أعمدة UPDATE بحيث لا يمكن لـ anon تغيير `status` أو `teacher_id` عبر التعديل (لا يوجد أي مكان في الكود الحالي يحتاج ذلك أصلًا).
3. **`harden_discount_codes_and_redeem_rpc`** — حذف صلاحية UPDATE المفتوحة، وإنشاء دالة آمنة `redeem_discount_code(code)` تتحقق من الصلاحية والحد الأقصى وتزيد العدّاد بشكل ذرّي (يحل أيضًا مشكلة "شرط السباق" الموجودة أصلًا في الكود القديم، حيث كان يمكن لعدة أشخاص استخدام نفس الكود في نفس اللحظة ويتجاوزون `max_uses`).
4. **`remove_anon_access_to_financial_tables`** — إزالة كل وصول anon نهائيًا من 4 جداول الإدارة المالية (لا حاجة له إطلاقًا، لوحة المالية تعمل فقط بحساب الإدارة المسجَّل دخول).
5. **`scope_platform_events_and_rewards_tiers`** — إضافة سياسة إدارة صحيحة (لم تكن موجودة أصلًا!) + قراءة عامة للفعّال فقط.
6. **`cleanup_redundant_policies_site_settings_subjects_profiles`** — حذف سياسات مكررة بلا أثر وظيفي.
7. **`scope_analytics_events_admin_access`** — حذف السياسات المفتوحة، سياسة إدارة موحّدة تحل محل القديمة.
8. **`protect_survey_responses_and_announcement_views`** — منع أي شخص من قراءة تقييمات المدرسين (إلا الإدارة)، وتقييد أعمدة UPDATE في `announcement_views`.
9. **`fix_function_search_paths_and_drop_unused_objects`** — تثبيت `search_path` في 4 دوال (نصيحة أمان قياسية)، حذف الـ view غير المستخدَم `analytics_summary` (كان بخاصية SECURITY DEFINER خطيرة)، حذف فهرس مكرر.

ثم أثناء التحقق النهائي (اختبرت كل استعلام حقيقي في الكود مقابل الصلاحيات الجديدة عمودًا‑عمودًا)، اكتشفت 4 حالات كانت ستكسر وظائف حقيقية في الموقع لو تُركت، فأصلحتها فورًا:

10. **`restore_anon_update_policy_on_teachers`** — نسيت أول مرة إعادة سياسة UPDATE بعد حذف القديمتين، وهذا كان سيمنع "حفظ الملف الشخصي" وتحديث النقاط بالكامل. تم تدارُكها.
11. **`widen_site_settings_policy_to_include_points_config`** — السياسة الآمنة كانت تسمح فقط بقراءة `launch_banner_visible`، لكن مفاتيح `points_ad_card_view` وأخواتها (من لوحة "نقاط التفاعل" بالإدارة) تحتاج أيضًا أن تُقرأ من متصفح كل زائر. تم توسيعها لتشمل `points_%` فقط.
12. **`restore_survey_responses_hasresponded_check`** — لوحة المدرس تتحقق "هل أجبت على هذا الاستطلاع من قبل؟" عبر قراءة anon، فحذف صلاحية القراءة بالكامل كان سيجعل الاستطلاع يظهر للمدرس كل مرة حتى بعد إجابته. أعدت صلاحية قراءة **مقتصرة على 3 أعمدة فقط** (`id, survey_id, teacher_id`) بدون `star_rating`/`answers` (المحتوى الفعلي يبقى سرّيًا).
13. **`restore_ads_select_for_teacher_own_dashboard`** — لوحة المدرس تعرض إعلاناته (معلّق/نشط/مرفوض) معًا بدون فلترة حالة، فتقييد القراءة على `status='active'` فقط كان سيُخفي إعلاناته المعلّقة والمرفوضة عن نفسه في لوحته! أعدت سياسة قراءة عامة (محتوى الإعلان نفسه ليس حسّاسًا مثل كلمات المرور أو البيانات المالية، فهذا تنازل معقول ومقصود).

### 1.5 كيف تأكدت أن كل شيء ما زال يعمل

هذا كان الجزء الأطول: بدل الافتراض، استخرجت **كل سطر** في كل ملفات الموقع يستدعي أي جدول عدّلته (`from('teachers')`, `from('ads')`, إلخ) وقارنت كل عمود مطلوب مع قائمة الأعمدة الجديدة المسموحة، عمودًا عمودًا. هذا بالضبط ما كشف الحالات الأربع أعلاه قبل أن تصل لك كأخطاء حية. بعد كل الإصلاحات، أعدت تشغيل نفس المطابقة ولم يتبقَّ أي عمود ناقص.

كذلك أعدت تشغيل **فاحص الأمان الرسمي من Supabase** (`get_advisors`) قبل وبعد: قبل الإصلاح كان هناك أكثر من 40 تنبيهًا من نوع "سياسة دائمًا صحيحة" تشمل كل الجداول الحرجة؛ بعد الإصلاح، كل التنبيهات المتبقية إما (أ) على جداول أُريدها مفتوحة عمدًا وبأمان (مثل `refund_requests`/`subscription_requests` اللذان كانا مصمَّمين بشكل صحيح أصلًا — INSERT فقط بدون SELECT)، أو (ب) على جداول منخفضة الحساسية لم تكن أولوية (`interaction_logs`, `survey_dismissals`, `package_settings`)، أو (ج) تحذيرات متوقّعة عن الدالتين الآمنتين الجديدتين نفسيهما (وهذا طبيعي، فهذا بالضبط الغرض منهما — أن تُستدعيا بأمان من anon).

### 1.6 ما لم يُصلَح بعد عمدًا (قيود معمارية حقيقية — تحتاج قرارك)

**السبب الجذري الأعمق لكل هذا** هو أن **المدرسين لا يستخدمون Supabase Auth الحقيقي** — تسجيل دخولهم كله مبني على جدول عادي + جلسة JS في `sessionStorage`، وكلهم (المدرسون والزوار العاديون) يشتركون في **نفس مفتاح anon العام** بلا أي طريقة حقيقية لتمييز "هذا المدرس رقم كذا" من "أي زائر عشوائي" على مستوى قاعدة البيانات. لهذا:

- **حذف/تعديل إعلان معيّن**: ما زال أي شخص يعرف `id` الإعلان يقدر يحذفه أو يعدّل وصفه/سعره (لكن ليس حالته أو صاحبه بعد اليوم). الحل الكامل الوحيد هو نقل تسجيل دخول المدرسين لـ Supabase Auth الحقيقي (تمامًا كما تفعل الإدارة الآن)، بحيث تستخدم السياسات `auth.uid()` الحقيقي بدل الاعتماد على فلترة الواجهة الأمامية فقط.
- محتوى الإعلانات نفسه (وليس بيانات الاعتماد) أصبح مقروءًا للجميع بلا قيد حالة — قرار مقصود لأن لوحة المدرس تحتاجه، وليس حسّاسًا مثل كلمات المرور.

هذا مشروع أكبر (نقل نظام الدخول كاملًا) أنصح به بشدة كخطوة تالية، لكنه يحتاج جلسة عمل مخصصة له لأنه يمس تسجيل الدخول وربما آلاف السطور، ولم أطبّقه اليوم حتى لا أكسر تسجيل الدخول الحالي بدون موافقتك واختبار كافٍ.

### 1.7 أشياء صغيرة لاحظتها ولم تحتج إصلاح فوري (اذكرها لعلمك فقط)

- عمود `teachers.plain_password` غير مُستخدَم حاليًا في أي مكان بالكود، وكلا الحسابين الحاليين (`مصعب عراقي رضوان` و`test`) فارغ فيهما هذا العمود — لكنه "قنبلة موقوتة": لو مليته يدويًا يومًا ما، ستنكشف كلمات المرور صراحة رغم كل الإصلاحات أعلاه لأن هذا عمود إضافي منفصل. أنصح بحذف العمود نهائيًا من الجدول ما دام غير مُستخدَم.
- جدولا `app_admins` و`app_sessions` موجودان في القاعدة (بهما RLS مفعّل لكن بلا أي سياسة، فهما معطَّلان فعليًا وآمنان كما هما) لكن لا يستخدمهما أي كود حاليًا. يبدو أنهما بداية تصميم لنظام جلسات حقيقي (بعمود `token_hash` و`role`!) — بالضبط نوع الحل الصحيح — لكنه توقف قبل أن يُربط بالواجهة. إما أكمل هذا المسار مستقبلًا (سيحل مشكلة القسم 1.6 نهائيًا)، أو احذف الجدولين لتفادي اللبس.
- Storage bucket العام `uploads` يسمح "بعرض قائمة" كل الملفات المرفوعة (وليس فقط الوصول لملف معروف الرابط). بما أنها صور إعلانات عامة أصلًا فالخطورة منخفضة، لكن يمكن إغلاق خاصية "العرض" لو أردت.
- ميزة "Leaked Password Protection" (فحص كلمات مرور الإدارة مقابل قاعدة بيانات كلمات مرور مسرّبة) معطّلة في إعدادات Supabase Auth — هذه تُفعَّل من لوحة Supabase مباشرة (Authentication → Policies)، وليست شيئًا أقدر أُغيّره عبر SQL.
- ملف `netlify.toml` موجود في المشروع لكن موقعك مستضاف فعليًا على GitHub Pages (كل الروابط الأساسية `canonical` تشير لـ `mosaabline11-sketch.github.io`) — هذا الملف بلا أي تأثير على استضافتك الحالية، يمكن حذفه بأمان.

---

## 2) مشكلة منطقية مهمة: "الرسالة الخاصة" ليست خاصة فعلًا

في `admin.html`، دالة `promptDirectMessage(teacherId, teacherName)` تعرض للإدارة نصًّا يقول "اكتب رسالتك للمدرس [الاسم]"، وتحفظها كـ:
```js
{ target: 'teachers', title: `رسالة خاصة لـ ${teacherName}`, message: `[خاص] ${msg}` }
```
لكن جدول `notifications` ليس فيه عمود لربط الرسالة بمدرس مُحدَّد — فقط `target` عام (`teachers` أو `all`). أي أن هذه "الرسالة الخاصة" **تظهر فعليًا لكل المدرسين في جرس الإشعارات لديهم**، وليس فقط للمدرس المقصود، رغم اسمها. هذا تناقض حقيقي بين ما تفعله الميزة وما تدّعيه واجهتها. الحل: إما إضافة عمود `teacher_id` لجدول `notifications` وتصفية العرض في `dashboard_logic.js` حسب المدرس الحالي، أو إعادة تسمية الميزة لتوضيح أنها تُرسل كإشعار عام يبدأ بـ "[خاص]" فقط شكليًا وليس فعليًا.

---

## 3) مشكلة منطقية: نظام النقاط قد يعطي صفرًا للجميع دائمًا

في `config.js`، دالة `_getPointsConfig()`:
```js
const n = Number(val);
result[t] = isNaN(n) ? DEFAULT_POINTS[t] : n;
```
لو لم يفتح المدير أبدًا لوحة "🎯 نقاط التفاعل" في `admin.html` ويضغط حفظ (حتى لو القيم المعروضة افتراضيًا تبدو صحيحة)، فإن `getSiteSetting()` يُرجع `null` لعدم وجود صف أصلًا — و`Number(null)` في JavaScript يساوي **صفر**، وليس `NaN`. بما أن `isNaN(0)` تُرجع `false`، فإن الشرط أعلاه **لا يستخدم القيمة الافتراضية**، بل يضع `0` مباشرة! يعني: **كل زيارة إعلان، وكل ضغطة واتساب، تمنح صفر نقطة خبرة (XP) لكل مدرس، ما لم تكن قد فتحت هذه اللوحة وضغطت "حفظ النقاط" مرة واحدة على الأقل من قبل.** هذا خلل حقيقي مستقل تمامًا عن أي تعديل قمت به اليوم في الصلاحيات. **الحل السريع الآن من جهتك:** ادخل لوحة الإدارة → إعدادات الموقع → نقاط التفاعل → اضغط "حفظ النقاط" مرة واحدة فقط (حتى لو لم تغيّر أي رقم) — سيُنشئ هذا الصفوف الناقصة في القاعدة وتُحل المشكلة فورًا. الحل البرمجي الصحيح على المدى الطويل: تغيير الشرط في `config.js` إلى ما يشبه `result[t] = (val === null || isNaN(n)) ? DEFAULT_POINTS[t] : n;`.

---

## 4) ملاحظات التصميم (كمبيوتر/هاتف)

الخبر الجيد: ملف `style.css` يحمل آثار عدة مراحل تصحيح جادة لمشاكل الـ overflow الأفقي على الهاتف (تعليقات "V5"، "V6.3"، "V9" داخل الملف)، وفعليًا لم أجد كسورًا جديدة كبيرة في التخطيط بعد فحص كل نقاط الـ breakpoints الرئيسية (`dashboard.html`, `admin.html`, `search.html`, `financial-dashboard.html`). الملاحظات المتبقية أصغر:

- **روابط واتساب/فيسبوك في `buildContactButtons()` (بملف `config.js`) وفي `join.html` تفتح بـ `target="_blank"` بدون `rel="noopener"`.** هذه ثغرة صغيرة معروفة باسم reverse tabnabbing: الصفحة المفتوحة حديثًا (رابط واتساب/فيسبوك المدرس، وهو رابط يضعه المدرس نفسه) تقدر نظريًا تتحكم بتوجيه تبويب موقعك الأصلي لصفحة تصيّد. الإصلاح بسيط: أضف `rel="noopener"` لكل هذه الروابط، ولإضافة `'noopener'` كوسيط ثالث في `window.open(...)` بملف `join.html`.
- **علامتان تجاريتان مختلفتان (Tagline) تُستخدمان بالتبادل بلا توحيد:** "ابحث عن مدرسك المثالي" (في `config.js` و`index.html`) مقابل "فهم أفضل.. مستقبل أفضل" (في شاشة splash وفي أعلى `search.html`). يستحسن توحيدهما لهوية بصرية أوضح.
- **قيمة `DEFAULT_ADS_LIMIT: 3` في `config.js`** لا تطابق أي باقة فعلية (كل الباقات حاليًا `ads_limit: 1`) — قيمة احتياطية ميتة عمليًا، يفضّل توحيدها مع الباقات أو حذفها.
- **`search.html` يعيد تعريف قائمة الصفوف الدراسية (`STAGES`) من الصفر** رغم وجود نفس البيانات بالضبط في `GRADE_SECTIONS` بملف `config.js` المُحمَّل مسبقًا. لو أضفت صفًا دراسيًا جديدًا يومًا ما، يجب تذكّر تعديله في **مكانين منفصلين** وإلا اختلفت صفحة البحث عن باقي الموقع. يفضَّل حذف `STAGES` من `search.html` واستخدام `GRADE_SECTIONS` مباشرة.
- **رقم واتساب الإدارة `+966 54 839 0129` بصيغة دولية سعودية (966)**، بينما كل محتوى المنصة (المناهج، العملة "جنيه"، صيغة الهاتف `01xxxxxxxxx`، وكلمة "مصر" في كل صفحة) يستهدف **مصر حصرًا**. قد يكون هذا رقمك الشخصي فعلًا (ولا مشكلة حينها)، لكن يستحق تأكيدًا سريعًا منك أنه الرقم الصحيح المقصود، لأن أي طالب/ولي أمر مصري قد يتردد بالتواصل مع رقم بمقدمة دولية مختلفة عن بلده.
- `netlify.toml` (مذكور أعلاه في قسم الأمان) — غير فعّال على استضافتك الحالية.

---

## 5) هل يبدو الموقع "مصنوعًا بالذكاء الاصطناعي"؟

إجابة صريحة ومتوازنة لأن هذا ما طلبته: **لزائر أو طالب عادي، لا — الموقع يبدو منصة مصرية احترافية معقولة، ولا يوجد نص "Lorem Ipsum" متروك ولا تخطيط مكسور بشكل واضح.** لكن لمطوّر أو لأي شخص يقرأ الكود مباشرة، هناك مؤشرات واضحة على أن جزءًا كبيرًا من التطوير تم بمساعدة الذكاء الاصطناعي:

- استخدام مكثّف جدًا للإيموجي في كل زر وعنوان تقريبًا (📚 📋 💰 🎯 ✅ ❌ ⚠️ 🚀) في كل صفحات الموقع دون استثناء تقريبًا، حتى الصفحات الرسمية (الشروط، الخصوصية).
- فواصل تعليقات بخطوط متطابقة تمامًا (`══════════`) متكررة حرفيًا عبر عشرات الملفات.
- سرعة إضافة الميزات (نظام تلعيب كامل، نظام اشتراكات وخصومات، لوحة مالية، استطلاعات، إشعارات) خلال أشهر قليلة رغم أن قاعدة المستخدمين الفعلية حاليًا صغيرة جدًا (مدرّس واحد حقيقي + حساب تجربة) — نمط شائع عند التطوير بمساعدة الذكاء الاصطناعي حيث تكلفة بناء ميزة جديدة منخفضة جدًا.
- نمط "إضافة سياسة أحدث وأدق فوق القديمة بدل حذف القديمة" المكتشف في قسم الأمان، وهو أثر كلاسيكي لتعديلات تراكمية عبر عدة جلسات ذكاء اصطناعي بدون مراجعة شاملة نهائية (بالضبط الفحص الذي طلبته اليوم).

لو كنت تريد تقليل هذا الانطباع لزائر تقني/مستثمر محتمل يفحص الكود: قلّل كثافة الإيموجي في الصفحات الرسمية خصوصًا، ونوّع بنية الصفحات بدل التكرار شبه الحرفي للقالب نفسه.

---

## 6) الملفات المرفقة مع هذا التقرير

1. **`login.html`** — نسخة مصححة (السطور المتغيرة فقط: دالة `loginTeacher`) لاستخدام دالة `teacher_login` الآمنة الجديدة. **يجب رفعها.**
2. **`join.html`** — نسخة مصححة (استخدام كود الخصم فقط) لاستخدام دالة `redeem_discount_code` الآمنة الجديدة. **يجب رفعها.**
3. **`security_migrations_2026-07-08.sql`** — نسخة كاملة من كل الـ 13 migration المطبَّقة اليوم على قاعدة بياناتك، للأرشيف (بنفس أسلوب ملفاتك السابقة مثل `fix_rls_secure.sql`)، تحديثًا لـ `SETUP_ORDER.md`.

كل التغييرات الأخرى المذكورة في القسم 1.4 **مُطبَّقة بالفعل على قاعدة بياناتك الحية الآن** — لا تحتاج تشغيل أي SQL بنفسك، فقط رفع الملفين أعلاه.

---

## 7) ملخص الأولويات

| # | البند | الحالة |
|---|---|---|
| 1 | تسريب كلمات مرور المدرسين + تعديل بياناتهم من أي زائر | ✅ تم الإصلاح على القاعدة الحية |
| 2 | نشر/تعديل/حذف أي إعلان من أي زائر بلا مراجعة | ✅ تم الإصلاح على القاعدة الحية |
| 3 | كل بيانات الإدارة المالية مكشوفة وقابلة للتعديل من أي زائر | ✅ تم الإصلاح على القاعدة الحية |
| 4 | تزوير أكواد الخصم | ✅ تم الإصلاح على القاعدة الحية (+ دالة RPC جديدة) |
| 5 | رفع `login.html` و`join.html` المصححين | 🔴 **مطلوب منك الآن** |
| 6 | ميزة "الرسالة الخاصة" ليست خاصة فعلًا | 🟡 يحتاج قرار تصميم منك |
| 7 | نظام النقاط قد يمنح صفرًا دائمًا | 🟡 افتح لوحة "نقاط التفاعل" واضغط حفظ مرة واحدة الآن |
| 8 | نقل تسجيل دخول المدرسين لـ Supabase Auth حقيقي | 🔵 توصية مستقبلية كبيرة (تحل قيود الملكية المتبقية) |
| 9 | ربط نظام `upsert_interaction_safe` الحقيقي بدل الاعتماد على فحص المتصفح فقط | 🔵 توصية مستقبلية |
| 10 | حذف عمود `plain_password` غير المستخدَم، وجدولي `app_admins`/`app_sessions` أو إكمال تصميمهما | 🔵 تنظيف اختياري |
| 11 | `rel="noopener"` على روابط واتساب/فيسبوك، توحيد الـ Tagline، تأكيد رقم الواتساب | 🟢 تحسينات صغيرة |
