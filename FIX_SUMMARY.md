# ملخص إصلاحات مدرسك

تم الإصلاح في: 2026-05-20 02:03

## التعديلات التي تمت

1. إصلاح بحث الصفحة الرئيسية:
   - تمت إضافة حقل بحث `hero-search` داخل كارت الفلترة.
   - تم منع انهيار `applySearch()` لو العنصر غير موجود لأي سبب.
   - زر تطبيق الفلترة يعمل الآن مع البحث + المادة + الصف.

2. إصلاح ظهور زر الإدارة:
   - تم تحويل `updateNavAuth()` إلى دالة async.
   - تم استخدام `await Auth.getAdminSession()` بدل التعامل مع Promise كأنه نتيجة جاهزة.
   - تم إخفاء روابط الإدارة افتراضيًا حتى يتم التأكد من صلاحية الأدمن.

3. إصلاح دالة `trackedContactHref()`:
   - تم إصلاح بناء `href` و `onclick` بشكل صحيح وآمن.

4. تحسين التعامل مع الشعار:
   - لم يتم إنشاء شعار جديد.
   - لو ملف `logo.png` غير موجود، يتم إخفاء الصورة المكسورة وإظهار نص بديل في بعض أماكن البراند.

## أشياء لم يتم تغييرها عن قصد

- ملف `netlify.toml` لم يتم تعديله لأنه غير مهم طالما الموقع ليس مرفوعًا على Netlify.
- `CONTACT_WHATSAPP` بقي فارغًا في `config.js` لأن رقم الإدارة لم يتحدد بعد.
- ملفات SQL/RLS لم يتم تغييرها جذريًا حتى لا تنكسر لوحة الإدارة/المدرس في النسخة الحالية. يفضّل عمل مرحلة أمان منفصلة لاحقًا.
